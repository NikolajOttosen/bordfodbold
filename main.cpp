#include <fstream>
#include <vector>
#include <stdlib.h>
#include "players.h"
using namespace std;

vector<string> players;
vector<int>    rating;
void updaterating(string P1, string P2)
{
    ;
}
int main()
{
    updaterating(players[0], players[1]);

    return 0;
}
